#key_name <- define in tower with survey/extra_vars
#instance_type: t3.large <- define in tower with survey/extra_vars
desired_targets:
  # how many centos SSH hosts you want
  cent_ssh: "{{ tower_cent_ssh }}"
  # how many Cisco IOS simulator hosts you want
  cisco_ios: 0
